<h1>Look at all the $$$!</h1>

<h3>Donations</h3>
	<% @donations.each do |d| %>
				<tr>			
					<td class="links" class="rows">
						<%= link_to d.name, d,'data-no-turbolink' => true %></td>
					<td class="rows"><%= d.org %></td>
					<td class="rows"><%= d.amount%></td>
					</td>
			
				</tr>

	
				<%end %>

