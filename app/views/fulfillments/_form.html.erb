<%= form_for(@fulfillment) do |f| %>
  <% if @fulfillment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fulfillment.errors.count, "error") %> prohibited this fulfillment from being saved:</h2>

      <ul>
      <% @fulfillment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>
 
  <div class="field">
    <% f.label :amount %>
    <% f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit(:class  => "btn btn-primary") %>
  </div>
    <% end %>